# Команда /admin для админ-панели

## ✅ Реализовано

### Хендлер команды
- Файл: `bot/handlers/admin.py`
- Команда: `/admin`
- Фильтр: `AdminFilter()` - проверка прав администратора
- Действие: Запуск админ-панели (диалог `AdminMenu.MAIN`)

### Регистрация
- Хендлер зарегистрирован в `bot/main.py`
- Диалоги зарегистрированы через `DialogRegistry`
- Все админские диалоги доступны через меню

## 📋 Использование

1. Администратор отправляет команду `/admin` в бот
2. Бот проверяет права доступа через `AdminFilter`
3. Если пользователь является администратором - открывается админ-панель
4. Если нет - команда игнорируется (можно добавить сообщение об ошибке)

## 🔧 Настройка

Администраторы указываются в `.env` файле:

```env
ADMIN_IDS=123456789,987654321
```

Где `123456789` и `987654321` - это Telegram ID администраторов (через запятую).

## 📝 Структура

```
bot/
├── handlers/
│   └── admin.py          # Хендлер команды /admin
├── dialogs/
│   └── admin/
│       ├── menu.py       # Админское меню
│       ├── games.py      # Управление играми
│       ├── profiles.py   # Управление анкетами
│       └── orders.py     # Управление заказами
└── filters/
    └── admin.py          # Фильтр проверки админа
```

## ⚠️ Примечания

- Команда доступна только администраторам
- При запуске админ-панели очищается стек диалогов (`RESET_STACK`)
- Все диалоги связаны через состояния и могут переключаться между собой

