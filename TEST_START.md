# Тестовый запуск бота

## ✅ Что сделано

1. Упрощен `bot/main.py`:
   - Закомментированы все сложные импорты (БД, диалоги, сервисы)
   - Оставлен только базовый роутер с командой `/start`
   - Добавлен простой хендлер для `/start` с тестовым сообщением

2. Смягчена проверка токена в `bot/config.py`:
   - Закомментирована проверка BOT_TOKEN для тестового запуска

## 🚀 Как запустить

1. Убедитесь, что у вас есть `.env` файл с токеном:
   ```env
   BOT_TOKEN=your_bot_token_here
   ```

2. Запустите бота:
   ```bash
   python -m bot.main
   ```

3. Отправьте команду `/start` боту в Telegram

4. Бот должен ответить: "✅ Бот запущен и работает! Это тестовое сообщение."

## 📝 Что закомментировано

- Инициализация базы данных
- ReminderService
- Админские диалоги
- Админские хендлеры
- DialogRegistry

Все это можно раскомментировать после успешного тестового запуска.

